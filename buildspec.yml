version: 0.2

phases:
  install:
    commands:
      - echo "Install phase started"
  build:
    commands:
      - echo "Build started on $(date)"
      - zip -r deploy.zip . -x "*.git*" -x "terraform/*"

artifacts:
  files:
    - deploy.zip